# 🚀 Скрипты автоматизации для микросервисов

## 📋 Список доступных скриптов

### 🗄️ **Работа с базой данных:**
- **`setup_database.sh`** - создание всех баз данных MySQL
- **`run_migrations.sh`** - запуск миграций во всех сервисах

### 🔨 **Сборка проекта:**
- **`build_all.sh`** - сборка всех сервисов через `brewkit build`
- **`brewkit_commands.sh`** - запуск различных команд brewkit
  - `./brewkit_commands.sh test` - запуск тестов
  - `./brewkit_commands.sh check` - проверка линтером
  - `./brewkit_commands.sh generate` - генерация кода
  - `./brewkit_commands.sh build` - сборка (аналог build_all.sh)

### 🚀 **Полное развертывание:**
- **`deploy_all.sh`** - полный цикл: БД + миграции + сборка

### ▶️ **Запуск сервисов:**
- **`start_all.sh`** - запуск всех сервисов локально для тестирования
- **`stop_all.sh`** - остановка всех запущенных сервисов (создается автоматически)

## 🎯 Быстрый старт

### **Для нового проекта:**
```bash
# 1. Настройте .env файлы в каждом сервисе (уже созданы)
# 2. Полное развертывание
./deploy_all.sh
```

### **Только миграции:**
```bash
./run_migrations.sh
```

### **Только сборка:**
```bash
./build_all.sh
```

### **Тестирование:**
```bash
./brewkit_commands.sh test
```

## 📁 Структура .env файлов

Каждый сервис имеет свой .env файл с переменными в верхнем регистре и префиксом appID:

**Пример для user сервиса:**
```bash
USER_DB_HOST=localhost
USER_DB_PORT=3306
USER_DB_NAME=user_microservice
USER_DB_USER=root
USER_DB_PASSWORD=your_password
USER_DB_MAX_CONN=10
USER_LOG_LEVEL=info
USER_SERVE_GRPC_ADDRESS=:8081
USER_TEST_GRPC_ADDRESS=test:8081
```

## ⚠️ Требования

- **MySQL** - для работы с базой данных
- **brewkit** - для сборки проектов
- **Go** - для компиляции сервисов
- Настроенные .env файлы в каждом сервисе

## 🐛 Устранение неполадок

См. раздел "Возможные проблемы" в MIGRATIONS_README.md для решения типичных ошибок.