# 📚 Инструкция по запуску миграций

## ✅ Изменения

**Удалены тестовые таблицы** - миграции теперь создают только необходимые таблицы для работы микросервисов.

**Добавлена поддержка .env файлов** - все сервисы теперь используют godotenv для чтения переменных окружения из .env файлов с префиксом appID в верхнем регистре.

**Обновлена настройка подключения к БД** - добавлен параметр multiStatements=true в DSN для поддержки множественных запросов.

## 🎯 Быстрый старт

### 1. **Настройка базы данных MySQL**
```bash
# Запустите скрипт создания базы данных
./setup_database.sh
```

### 2. **Настройка переменных окружения**
Все .env файлы уже созданы для каждого сервиса с переменными в верхнем регистре и префиксом appID.

**Пример переменных для каждого сервиса:**

**Order Service (.env):**
```bash
ORDER_DB_HOST=localhost
ORDER_DB_PORT=3306
ORDER_DB_NAME=order_microservice
ORDER_DB_USER=root
ORDER_DB_PASSWORD=your_password
ORDER_DB_MAX_CONN=10
ORDER_LOG_LEVEL=info
ORDER_SERVE_GRPC_ADDRESS=:8081
ORDER_TEST_GRPC_ADDRESS=test:8081
```

**User Service (.env):**
```bash
USER_DB_HOST=localhost
USER_DB_PORT=3306
USER_DB_NAME=user_microservice
USER_DB_USER=root
USER_DB_PASSWORD=your_password
USER_DB_MAX_CONN=10
USER_LOG_LEVEL=info
USER_SERVE_GRPC_ADDRESS=:8081
USER_TEST_GRPC_ADDRESS=test:8081
```

И так далее для всех сервисов (payment, product, notification).

**Важно:** Переменные должны иметь префикс appID и быть в верхнем регистре!

### 3. **Запуск миграций**
```bash
# Запустите миграции для всех сервисов
./run_migrations.sh
```

### 4. **Сборка сервисов** (опционально)
```bash
# Сборка всех сервисов с помощью brewkit
./build_all.sh

# Или для конкретных команд brewkit:
./brewkit_commands.sh test     # запуск тестов
./brewkit_commands.sh check    # проверка линтером
./brewkit_commands.sh generate # генерация кода
```

### 5. **Полное развертывание** (все этапы сразу)
```bash
# Полное развертывание: БД + миграции + сборка
./deploy_all.sh
```

## 🔧 Ручной запуск миграций

Если нужно запустить миграции для конкретного сервиса:

```bash
# Order Service
cd order && go run cmd/order/main.go migrate

# User Service  
cd user && go run cmd/user/main.go migrate

# Payment Service
cd payment && go run cmd/payment/main.go migrate

# Product Service
cd product && go run cmd/product/main.go migrate

# Notification Service
cd notification && go run cmd/notification/main.go migrate
```

## 📊 Структура таблиц

После успешного выполнения миграций в базах данных будут созданы следующие таблицы:

### **order_microservice**
- `orders` - основная таблица заказов
- `order_items` - элементы заказов

### **user_microservice**
- `users` - пользователи системы

### **payment_microservice**
- `payments` - платежи
- `wallets` - кошельки пользователей

### **product_microservice**
- `products` - товары/продукты

### **notification_microservice**
- `notifications` - уведомления
- `recipients` - получатели уведомлений

## ⚠️ Возможные проблемы

### **Ошибка подключения к БД**
- Проверьте настройки в `.env` файлах
- Убедитесь, что MySQL запущен и доступен
- Проверьте правильность логина и пароля

### **Ошибка "database does not exist"**
- Запустите `./setup_database.sh` для создания баз данных
- Или создайте базы вручную через MySQL консоль

### **Ошибка "access denied"**
- Проверьте права пользователя MySQL
- Убедитесь, что пользователь имеет права на создание таблиц

### **Ошибка "brewkit not found"**
- Убедитесь, что brewkit установлен и добавлен в PATH
- Проверьте установку: `which brewkit`

### **Ошибки при сборке**
- Проверьте наличие всех зависимостей Go
- Убедитесь, что все .env файлы настроены правильно
- Проверьте структуру проекта и наличие brewkit.jsonnet

### **Ошибки миграций с multiStatements**
- Если возникают проблемы с multiStatements=true, удалите этот параметр из buildDSN() в config.go

## ✅ Проверка результата

После выполнения миграций можно проверить результат:

```sql
-- Подключитесь к каждой базе и проверьте таблицы
USE order_microservice;
SHOW TABLES;

USE user_microservice;  
SHOW TABLES;

-- и так далее для всех баз
```